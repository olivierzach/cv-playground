[project]
name = "mnist-playground"
version = "0.1.0"
description = "Local MNIST model factory and static artifact validator for the MNIST Playground."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests_py"]
